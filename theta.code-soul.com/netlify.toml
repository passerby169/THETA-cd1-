# Netlify 部署配置
# 注意：如果从仓库根目录部署，需要在 Netlify 设置中指定：
# Base directory: theta.code-soul.com
# Build command: pnpm install && pnpm build
# Publish directory: theta.code-soul.com/.next

[build]
  command = "pnpm install && pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "8"
  NPM_FLAGS = "--legacy-peer-deps"

# Next.js 特定配置
[[plugins]]
  package = "@netlify/plugin-nextjs"
